#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""第一幕 L01-L04 内容。接着用户已有的 socket 实验往上长。"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from blocks import *  # noqa


# ================================================================ L01
def L01():
    blocks = [
        hook(
            "你已经写了 `tcp_client` 和 `tcp_server`,让两个进程在一台机器上说上了话。"
            "现在把问题问死一点:你 `send(\"hello\")` 之后,这 5 个字节在**到达对面 "
            "`recv` 之前**,到底被谁碰过、被加了什么、凭什么没送错机器、没送错进程?\n\n"
            "这一关不写新代码,而是把你已经跑通的那条连接,一层一层剖开看。"
        ),

        h("先要一个能盯着字节的工具"),
        p("肉眼看不见字节。但 Linux 自带的工具能。先确认你的两个程序还能连通,"
          "然后我们用 `ss` 看内核眼里这条连接长什么样。"),

        lab(
            "see-conn", "看内核眼里的一条连接",
            "这个实验起一个 TCP 服务器,连一个客户端,然后用 `ss -tanp` 把这条连接"
            "在内核里的样子打印出来。你会第一次看到「四元组」这个真实存在的东西。",
            script="l01_see_conn",
            cmd="./bin/tcp_server 18801 &\n"
                "./bin/tcp_client 127.0.0.1 18801 hello &\n"
                "ss -tanp | grep 18801",
            ask="同一个 18801 端口上,ss 会打印几行?为什么?",
            expect=(
                "=== 启动服务器 (端口 18801) ===\n"
                "=== 客户端连上并 hold 住 ===\n\n"
                "State    Recv-Q Send-Q  Local Address:Port   Peer Address:Port\n"
                "LISTEN   0      128     0.0.0.0:18801        0.0.0.0:*\n"
                "ESTAB    0      0       127.0.0.1:18801      127.0.0.1:52344\n"
                "ESTAB    0      0       127.0.0.1:52344      127.0.0.1:18801\n"
            ),
            explain=(
                "**三行,不是一行。** 这三行揭示了整个 TCP 的骨架:\n\n"
                "1. `LISTEN  0.0.0.0:18801` —— 这是你 `listen()` 出来的**监听套接字**。"
                "`0.0.0.0` 意思是「本机所有网卡的 18801 都算我的」。它不传数据,只负责生连接。\n"
                "2. `ESTAB  127.0.0.1:18801 <-> 127.0.0.1:52344` —— 服务器这一侧的**连接套接字**,"
                "就是 `accept()` 返回的那个新 fd。\n"
                "3. `ESTAB  127.0.0.1:52344 <-> 127.0.0.1:18801` —— 客户端那一侧。\n\n"
                "注意第 2、3 行的地址是**镜像**的。`52344` 是内核给客户端随机分的**临时端口**"
                "(你没 bind,所以内核替你选了一个)。这四个数 —— "
                "`(源IP, 源端口, 目的IP, 目的端口)` —— 就是**四元组**,TCP 用它唯一标识一条连接。"
            ),
        ),

        key(
            "一个监听端口能挂几万条连接,靠的就是四元组。目的地址都是 `服务器IP:18801`,"
            "但每个客户端的 `(源IP, 源端口)` 不同,所以内核能把它们分得清清楚楚。"
            "**「8888 端口只能被一个人用」是彻底的误解** —— 被独占的是*监听*,不是*连接*。",
            "为什么一个端口能扛几万连接"
        ),

        h("字节往下走:四层封装"),
        p("你的 `send()` 把数据交给内核之后,它不是直接上网线的。每往下一层,就被套一个头 —— "
          "像寄快递时先装进信封(写收件人),再装进邮包(写地址),再贴上路由单。"
          "这个过程叫**封装**,收端反过来一层层拆。"),

        fig("layers", "点播放:一个 HTTP 请求从应用层往下,每层套一个头"),

        p("这张图解释了一个你迟早会遇到的怪事:**`ping` 通了,但连不上端口。** "
          "`ping` 用的是网络层的 ICMP,只验证了「IP 层能到这台机器」。而连接端口要靠传输层的 TCP —— "
          "机器在,不代表那个端口上有进程在听,也不代表防火墙放行了传输层。"
          "这两件事分属不同层,所以会各自失败。"),

        table(
            ["层", "地址是什么", "回答的问题", "你代码里对应"],
            [
                ["应用层 HTTP", "URL / 路径", "要什么资源", "`GET /api/orders`"],
                ["传输层 TCP", "**端口**", "给哪个**进程**", "`bind(8888)` / `connect(...,8888)`"],
                ["网络层 IP", "**IP 地址**", "给哪台**机器**", "`127.0.0.1` / `inet_addr(...)`"],
                ["链路层 Ethernet", "MAC 地址", "下一跳哪块**网卡**", "内核和网卡驱动管,你碰不到"],
            ],
            title="四个地址,四个问题,别混"
        ),

        h("字节序:为什么要 htons"),
        p("你在 C 代码里写过 `htons(port)`,可能没细想为什么。这不是仪式,不加会真的连错端口。"),

        lab(
            "byteorder", "亲眼看字节序把端口改了",
            "端口 8888 在内存里的两个字节,大端机器和小端机器摆放顺序相反。"
            "网络规定一律用大端(网络字节序)。这个实验打印 8888 在转换前后的十六进制,"
            "让你看到 `htons` 到底动了什么。",
            script="l01_byteorder",
            ask="8888 = 0x22B8。x86 是小端。如果不转换直接发出去,对方会读成哪个端口?",
            expect=(
                "主机端口:            8888   = 0x22b8\n"
                "内存里的字节(小端):  b8 22\n"
                "htons 之后:          0x22b8 -> 网络序字节 22 b8\n"
                "如果忘了 htons,对方读到: 0xb822 = 47138  <- 连错端口!\n"
            ),
            explain=(
                "x86 是**小端**:低位字节放在低地址,所以 `0x22B8` 在内存里是 `B8 22`。"
                "网络传输规定用**大端**(高位在前)。`htons` = Host TO Network Short,"
                "把主机序转成网络序。**忘了转换,8888 会被对方读成 47138**,连接自然失败。\n\n"
                "为什么 Python 里你从来没写过 htons?因为它在 `('127.0.0.1', 8888)` 这个元组"
                "转成底层地址时**隐式**帮你做了。字节序转换照样发生,只是被藏起来了 —— "
                "这正是高级语言「把历史包袱封装掉」的典型例子,但**协议本质一点没变**。"
            ),
            xp=15,
        ),

        h("三种语言,同一件事"),
        p("你已经有 C 和 Python 版了。这里补上 Go,三个放一起看 —— 目的是让你确信:"
          "**变的只是语法糖的厚度,六个系统调用一个都没少。**"),

        poly(
            title="建立连接并发一句话(客户端)",
            cpp=(
                "int fd = socket(AF_INET, SOCK_STREAM, 0);\n"
                "struct sockaddr_in srv;\n"
                "memset(&srv, 0, sizeof(srv));\n"
                "srv.sin_family = AF_INET;\n"
                "srv.sin_port   = htons(8888);          // 字节序,显式\n"
                "inet_pton(AF_INET, \"127.0.0.1\", &srv.sin_addr);\n"
                "connect(fd, (struct sockaddr*)&srv, sizeof(srv));\n"
                "send(fd, \"hello\", 5, 0);\n"
                "close(fd);",
                "client.c",
                "最啰嗦,但每一步都摊开:填地址结构、字节序、强制类型转换。"
                "**这些啰嗦不是 C 故意刁难你,是它没有隐藏任何东西。**"
            ),
            py=(
                "import socket\n"
                "s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n"
                "s.connect(('127.0.0.1', 8888))   # 元组一步到位,字节序隐式\n"
                "s.sendall(b'hello')              # sendall 帮你循环发\n"
                "s.close()",
                "client.py",
                "`('127.0.0.1', 8888)` 一个元组顶了 C 的五行。`sendall` 是 `send` 的循环版。"
                "但 `AF_INET`、`SOCK_STREAM` 还在 —— 说明它们是**协议概念**,不是 C 的历史包袱。"
            ),
            go=(
                "package main\n"
                "import (\"net\")\n\n"
                "func main() {\n"
                "    conn, err := net.Dial(\"tcp\", \"127.0.0.1:8888\")\n"
                "    if err != nil { panic(err) }\n"
                "    defer conn.Close()\n"
                "    conn.Write([]byte(\"hello\"))   // Write 就是 send\n"
                "}",
                "client.go",
                "Go 更进一步:`net.Dial(\"tcp\", ...)` 把 socket+connect 合成一句。"
                "但底层它照样调 `socket()`/`connect()`。Go 的杀手锏不在这行,在**并发** —— L04 会看到。"
            ),
        ),

        note(
            "三种语言连同一个服务器,完全通。C 客户端能连 Python 服务器,Go 能连 C —— "
            "因为跑在线上的是 TCP/IP,和你用什么语言写没有半点关系。这件事你现在就能验证:"
            "`python3 py/tcp_server.py 8888` 然后用 C 的 `./bin/tcp_client 127.0.0.1 8888`。",
            "info", "跨语言互通"
        ),
    ]

    boss = [
        q("`ss` 显示同一个服务端口下有一行 `LISTEN` 和多行 `ESTAB`。这些 `ESTAB` 行为什么不冲突?",
          ["它们用了不同的端口号",
           "靠四元组 (源IP,源端口,目的IP,目的端口) 区分,目的端口相同但源端口不同",
           "内核给每个连接偷偷换了 IP",
           "其实会冲突,ss 显示有 bug"],
          1,
          "监听端口只有一个,但每条连接的**源端口**(客户端的临时端口)不同,四元组就不同。"
          "这是「一个端口扛几万连接」的根本原因。"),

        q("你 `ping 某服务器` 通了,但程序 `connect` 那个服务的 8080 端口失败。最不可能的原因是?",
          ["8080 端口上没有进程在 listen",
           "防火墙拦了 8080 的 TCP",
           "IP 层不通,包根本到不了这台机器",
           "服务进程崩了但机器还在"],
          2,
          "`ping` 走网络层 ICMP,它通了恰恰说明**IP 层是通的**、包能到这台机器。"
          "所以「IP 层不通」与「ping 通」矛盾,是最不可能的。连不上端口的原因都在传输层及以上。"),

        q("在 x86(小端)上忘了对端口调 `htons` 就发出去,后果是?",
          ["没影响,现代内核会自动纠正",
           "端口的两个字节顺序反了,对方解析成另一个端口,连不上",
           "程序直接崩溃",
           "只有跨机器才有问题,本机没事"],
          1,
          "字节序转换在**你的机器上**就发生。8888(0x22B8)不转换,对方按大端读成 0xB822=47138。"
          "本机回环也一样错 —— 因为发送方在写地址结构时就已经用错了序。"),
    ]

    write("L01", "一根线上的两个进程",
          "把你已经跑通的那条连接,一层层剖开:四元组、封装、字节序。",
          blocks, boss,
          boss_title="通关测试:字节的旅程",
          unlocks="- **浏览器 / App** 和 **负载均衡入口** 之间那条 HTTP 连线 —— "
                  "现在你知道这条线上跑的是什么了。")


# ================================================================ L02
def L02():
    blocks = [
        hook(
            "UDP 你也写过,它 `sendto` 完就撒手不管。TCP 却敢承诺「不丢、不乱、不重」。"
            "但网线本身是不可靠的 —— 会丢包、会乱序、会延迟。TCP 凭什么做到?\n\n"
            "答案是:它没有魔法,只是在**不可靠的 IP 之上**,用五个机制硬堆出了可靠。"
            "每一个机制都有代价,而这些代价就是你工作里遇到的性能问题的根源。"
        ),

        h("先看握手:可靠是从建立连接就开始的"),
        p("UDP 直接就发。TCP 却要先握三次手。为什么?因为「可靠」需要双方**对齐序号**"
          "—— 我从哪个字节开始数,你从哪个字节开始数,得先说好,后面的确认才有意义。"),

        fig("handshake", "点「下一步」逐步看:注意两侧的状态是在不同时刻变化的"),

        key(
            "看图里的关键时刻:客户端发出**第三个 ACK 时就已经 ESTABLISHED** 了,"
            "而服务端要等**收到**这个 ACK 才 ESTABLISHED。这半个 RTT 的错位,"
            "解释了为什么「客户端以为连上了、服务端还没准备好」这种时序 bug 真实存在。",
            "握手不是原子的"
        ),

        h("四次挥手与 TIME_WAIT —— 你一定会撞上的状态"),
        p("把上面那张图播放完,看后半段的关闭过程。重点是最后那个 **TIME_WAIT**:"
          "主动关闭连接的一方,要在这个状态**傻等约 60 秒**才释放端口。"),

        lab(
            "timewait", "谁先 close,谁进 TIME_WAIT",
            "这个实验让**客户端**先关闭连接,然后立刻用 `ss` 去看谁进了 TIME_WAIT。"
            "结果会告诉你一个精确的规则。",
            script="l02_timewait",
            ask="服务器和客户端,谁会进 TIME_WAIT?",
            expect=(
                "=== 服务器起在 18802,客户端连上发一句就 close ===\n\n"
                "State      Recv-Q Send-Q  Local Address:Port   Peer Address:Port\n"
                "LISTEN     0      128     0.0.0.0:18802        0.0.0.0:*\n"
                "TIME-WAIT  0      0       127.0.0.1:41100      127.0.0.1:18802\n\n"
                "注意 TIME-WAIT 那行的 Local 是 41100(客户端临时端口),不是 18802。\n"
            ),
            explain=(
                "TIME_WAIT 出现在**客户端**那一侧(Local 是客户端的临时端口 41100)。"
                "规则很干脆:**谁先调 `close()` 发出第一个 FIN,谁进 TIME_WAIT。**\n\n"
                "**为什么要傻等这 60 秒?** 两个原因:\n"
                "1. 最后那个 ACK 可能丢。对方没收到会重发 FIN,你得还在,才能补一个 ACK。\n"
                "2. 防止上一条连接的「迷路」数据包,在四元组被复用后串进新连接。\n\n"
                "**工作里的真实影响:** 如果你的**服务器**是主动关闭方(比如短连接、"
                "或者你主动 close 空闲连接),高并发下会积累**成千上万个 TIME_WAIT**,"
                "耗尽本地端口,新连接建不出来。这就是为什么 HTTP 要用 keep-alive、"
                "为什么让客户端而非服务端主动关闭是个讲究。"
            ),
        ),

        trap(
            "`CLOSE_WAIT` 大量堆积和 `TIME_WAIT` 完全是两码事,别搞混:\n\n"
            "- **TIME_WAIT 多** = 你**主动**关了很多连接,是正常现象(虽然可能要调优)。\n"
            "- **CLOSE_WAIT 多** = 对方关了,但你的程序**忘了 close** —— 这是**你的代码 bug**。"
            "收到对端 FIN(`recv` 返回 0)后你没调 `close()`,连接就永远卡在 CLOSE_WAIT。\n\n"
            "**面试高频、线上高频:看到一堆 CLOSE_WAIT,第一反应就是去查漏掉的 close。**",
            "TIME_WAIT vs CLOSE_WAIT"
        ),

        h("可靠的五个机制"),
        p("握手对齐了序号之后,数据传输阶段靠这五个机制保证可靠。"
          "重点不是记住名字,而是理解**每个机制在拿什么换什么**。"),

        table(
            ["机制", "解决什么", "代价 / 你观测到的现象"],
            [
                ["序号 seq", "乱序 → 能重排;重复 → 能去重", "每个字节都要编号"],
                ["确认 ACK", "对方怎么知道我收到了", "回程也要占带宽"],
                ["超时重传", "包丢了怎么办 —— 没等到 ACK 就重发", "**这是首字节延迟毛刺的元凶**"],
                ["滑动窗口", "不必发一个等一个,可以连发一批", "窗口大小限制了吞吐上限"],
                ["拥塞控制", "网络堵了就主动降速,别火上浇油", "**慢启动让新连接前几个 RTT 很慢**"],
            ],
        ),

        biz(
            "这两条直接对应你会遇到的性能问题:\n\n"
            "- **「接口偶尔卡一下,大部分时候很快」** —— 经典的**丢包 + 超时重传**。一个包丢了,"
            "TCP 要等一个 RTO(重传超时,常见 200ms 起)才重发,这一下就是几百毫秒毛刺。"
            "代码没问题,是网络。\n"
            "- **「短连接接口比长连接慢很多」** —— 每次新连接都要经历握手(1 RTT)+ 慢启动"
            "(拥塞窗口从很小开始爬)。keep-alive 复用连接就是省这两笔。",
            "把机制对应到线上现象"
        ),

        h("Go 的一个细节:连接就是 io.Reader/Writer"),
        poly(
            title="读一条连接直到对方关闭",
            cpp=(
                "char buf[1024];\n"
                "for (;;) {\n"
                "    ssize_t n = recv(fd, buf, sizeof(buf), 0);\n"
                "    if (n == 0) break;        // 对端 close,收到 FIN\n"
                "    if (n < 0)  { if (errno==EINTR) continue; break; }\n"
                "    /* 处理 buf[0..n) */\n"
                "}",
                "recv_loop.c",
                "`recv == 0` 是 TCP **唯一**的正常结束信号(收到 FIN)。这是必须背下来的。"
            ),
            py=(
                "while True:\n"
                "    data = conn.recv(1024)\n"
                "    if data == b'':      # 等价于 C 的 n == 0\n"
                "        break\n"
                "    # 处理 data",
                "recv_loop.py",
                "`b''`(空字节串)就是对端关闭。注意**不能**写 `if not data` 去判断 UDP —— "
                "UDP 收到空包也是 `b''`,含义完全不同。"
            ),
            go=(
                "buf := make([]byte, 1024)\n"
                "for {\n"
                "    n, err := conn.Read(buf)\n"
                "    if err == io.EOF { break }   // 对端 close\n"
                "    if err != nil    { break }\n"
                "    // 处理 buf[:n]\n"
                "}",
                "recv_loop.go",
                "Go 把「对端关闭」表达成 `io.EOF`,和读文件到末尾统一成一个概念。"
                "`net.Conn` 本身就是 `io.Reader`,所以能直接喂给 `io.Copy`、`bufio.Scanner` —— "
                "这个抽象在 L03 处理粘包时会非常好用。"
            ),
        ),
    ]

    boss = [
        q("线上服务器出现大量 `CLOSE_WAIT`,最可能的原因是?",
          ["对端服务器负载太高",
           "本机端口耗尽",
           "程序收到对端 FIN(recv 返回 0)后没有调用 close",
           "TIME_WAIT 太多导致的连锁反应"],
          2,
          "CLOSE_WAIT 是「对方已关,我方未关」的状态。它堆积 = 代码在 recv 到 0 之后"
          "没有 close 掉连接。这是本方的代码 bug,和对端、端口都无关。"),

        q("关于 TIME_WAIT,下面哪句是对的?",
          ["它出现在被动关闭连接的一方",
           "它出现在主动调用 close() 发出第一个 FIN 的一方,持续约 2×MSL",
           "它是一种错误状态,应该想办法消除",
           "服务器一定不会进入 TIME_WAIT"],
          1,
          "谁先 close 谁进 TIME_WAIT,持续约 60 秒(2×MSL)。它是**正常且必要**的,"
          "作用是兜住丢失的最后 ACK、隔离迷路的旧包。服务器如果是主动关闭方,一样会进。"),

        q("接口平时 5ms,偶尔某次 200ms,其余一切正常。最可能是?",
          ["代码里有个慢函数偶尔被触发",
           "数据库偶尔慢查询",
           "网络丢包触发了 TCP 超时重传",
           "GC 停顿"],
          2,
          "『大部分快、偶尔一次跳到几百毫秒、量级恰好是 RTO』是丢包重传的典型指纹。"
          "当然另几项也可能,但『整齐的几百毫秒毛刺』最经典的解释是超时重传 —— "
          "这时该去看网络和重传计数,而不是逐行读代码。"),
    ]

    write("L02", "TCP 凭什么叫「可靠」",
          "可靠不是魔法:握手对齐序号,五个机制堆出不丢不乱,每个机制都有你能观测的代价。",
          blocks, boss,
          boss_title="通关测试:可靠的代价",
          unlocks="- 你现在能读懂 `ss -tan` 的每一种状态了,这是排查线上连接问题的基本功。")


# ================================================================ L03
def L03():
    blocks = [
        hook(
            "你在实验五已经亲眼见过:客户端 `send` 五次,服务器 `recv` 一次就全收到了。"
            "消息边界消失了。这不是 bug,是 TCP 的**定义** —— 它是字节流,不是消息流。\n\n"
            "于是每一个基于 TCP 的协议,做的第一件事都是**发明一个重新划出边界的办法**。"
            "HTTP、Redis、MySQL、gRPC,无一例外。这一关,你来发明一个,然后就永远理解了它们。"
        ),

        h("再看一眼这个现象"),
        fig("stream", "点「发 5 个包」:TCP 边界消失成一条流,UDP 每个包独立到达"),

        p("左边 TCP:五个方块流进缓冲区就融成一条连续的带,收端 `recv` 一把全捞上来。"
          "右边 UDP:五个方块各飞各的,`recvfrom` 五次,一次一个,一字节不差。"
          "**这就是「流」和「数据报」的全部区别。**"),

        lab(
            "sticky", "在你本机复现粘包",
            "跑一个客户端连发 5 个小包,看服务器收了几次。这个实验直接用你已有的 "
            "`tcp_client --burst`。",
            script="l03_sticky",
            cmd="./bin/tcp_server 18803 &\n"
                "./bin/tcp_client 127.0.0.1 18803 --burst 5",
            ask="5 次 send,服务器会 recv 几次?",
            expect=(
                "=== 服务器 18803,客户端连发 5 个 6 字节的包 ===\n\n"
                "客户端:\n"
                "    send() \"PKT-00\" (6 字节)\n"
                "    send() \"PKT-01\" (6 字节)\n"
                "    send() \"PKT-02\" (6 字节)\n"
                "    send() \"PKT-03\" (6 字节)\n"
                "    send() \"PKT-04\" (6 字节)\n"
                "服务器:\n"
                "    recv() = 30 字节: \"PKT-00PKT-01PKT-02PKT-03PKT-04\"   <- 一次全收到\n"
            ),
            explain=(
                "5 次 `send` → 1 次 `recv`,30 字节挤在一起。反过来也会发生:"
                "发一个 10KB 的大包,接收方可能要 `recv` 好几次才收全(**拆包**)。\n\n"
                "根本原因:TCP 只保证「字节的顺序不乱、不丢」,**从不保证「你发几次我收几次」**。"
                "内核发送缓冲区攒够了就打包发走,和你调用 `send` 的次数无关。\n\n"
                "**结论刻进脑子:应用层必须自己定义消息边界。** 下面看两种主流做法。"
            ),
        ),

        h("两种划边界的办法"),
        p("整个业界就这两招,所有协议都是它们的变体:"),

        table(
            ["做法", "怎么做", "谁在用", "适合"],
            [
                ["**长度前缀**", "先发 4 字节表示长度 N,再发 N 字节内容", "gRPC、Thrift、Redis(混用)、绝大多数 RPC",
                 "二进制协议、大消息、性能敏感"],
                ["**分隔符**", "用 `\\r\\n` 之类分隔,读到分隔符算一条", "HTTP 头、Redis 命令、SMTP",
                 "文本协议、人类可读、消息不含分隔符时"],
            ],
        ),

        h("动手:实现长度前缀协议"),
        p("这是最重要的一段代码。看三种语言怎么写「发一条带长度头的消息」和"
          "「**完整读出**一条消息」。注意读的那一半 —— 那个 `read_exactly` 循环,"
          "是新手最容易写错、也是线上 bug 最多的地方。"),

        poly(
            title="发送:4 字节大端长度头 + 内容",
            cpp=(
                "// 发一条消息:先 4 字节网络序长度,再内容\n"
                "int send_msg(int fd, const char *data, uint32_t len) {\n"
                "    uint32_t nlen = htonl(len);              // 长度也要转字节序!\n"
                "    if (send_all(fd, (char*)&nlen, 4) < 0) return -1;\n"
                "    return send_all(fd, data, len);\n"
                "}",
                "framing.c",
                "长度头本身也是多字节整数,**也要 htonl**。这个坑很多人踩。"
            ),
            py=(
                "import struct\n\n"
                "def send_msg(sock, data: bytes):\n"
                "    # '>I' = 大端 4 字节无符号。struct 帮你处理字节序\n"
                "    sock.sendall(struct.pack('>I', len(data)) + data)",
                "framing.py",
                "`struct.pack('>I', ...)` 里的 `>` 就是大端。Python 把字节序做成了格式串里的一个字符。"
            ),
            go=(
                "import (\"encoding/binary\"; \"net\")\n\n"
                "func sendMsg(conn net.Conn, data []byte) error {\n"
                "    var hdr [4]byte\n"
                "    binary.BigEndian.PutUint32(hdr[:], uint32(len(data)))\n"
                "    if _, err := conn.Write(hdr[:]); err != nil { return err }\n"
                "    _, err := conn.Write(data)\n"
                "    return err\n"
                "}",
                "framing.go",
                "`binary.BigEndian` 把「大端」写成了显式的类型,读代码的人一眼知道字节序。"
            ),
        ),

        poly(
            title="接收:必须循环读够,不能假设一次到位",
            cpp=(
                "// read_exactly:一定读满 n 字节才返回。这是重点。\n"
                "int read_n(int fd, char *buf, size_t n) {\n"
                "    size_t got = 0;\n"
                "    while (got < n) {\n"
                "        ssize_t r = recv(fd, buf + got, n - got, 0);\n"
                "        if (r == 0) return 0;               // 对端关闭\n"
                "        if (r < 0) { if (errno==EINTR) continue; return -1; }\n"
                "        got += r;\n"
                "    }\n"
                "    return 1;\n"
                "}\n"
                "// 读一条消息:先读 4 字节长度,再按长度读内容\n"
                "int recv_msg(int fd, char *out, uint32_t *len) {\n"
                "    uint32_t nlen;\n"
                "    if (read_n(fd, (char*)&nlen, 4) <= 0) return -1;\n"
                "    *len = ntohl(nlen);\n"
                "    return read_n(fd, out, *len);\n"
                "}",
                "framing.c",
                "`read_n` 循环到读满为止。**没有这个循环,大消息就会被截断** —— 拆包就是这么坑人的。"
            ),
            py=(
                "import struct\n\n"
                "def read_n(sock, n: int) -> bytes:\n"
                "    buf = b''\n"
                "    while len(buf) < n:\n"
                "        chunk = sock.recv(n - len(buf))\n"
                "        if not chunk:                # 对端关闭\n"
                "            raise ConnectionError('对端在读完前关闭了')\n"
                "        buf += chunk\n"
                "    return buf\n\n"
                "def recv_msg(sock) -> bytes:\n"
                "    (length,) = struct.unpack('>I', read_n(sock, 4))\n"
                "    return read_n(sock, length)",
                "framing.py",
                "即便是 Python,`recv(n)` 也**不保证**返回 n 字节 —— 它返回「现在能给的」。"
                "所以 `read_n` 的循环一个都不能省。"
            ),
            go=(
                "import (\"encoding/binary\"; \"io\"; \"net\")\n\n"
                "func recvMsg(conn net.Conn) ([]byte, error) {\n"
                "    var hdr [4]byte\n"
                "    if _, err := io.ReadFull(conn, hdr[:]); err != nil {  // 就是 read_n\n"
                "        return nil, err\n"
                "    }\n"
                "    n := binary.BigEndian.Uint32(hdr[:])\n"
                "    data := make([]byte, n)\n"
                "    _, err := io.ReadFull(conn, data)                     // 循环读够\n"
                "    return data, err\n"
                "}",
                "framing.go",
                "Go 标准库直接给了 `io.ReadFull` —— 它就是封装好的 `read_n`。"
                "**这说明「循环读够」是个如此普遍的需求,以至于标准库专门为它做了个函数。**"
            ),
        ),

        key(
            "把这句话背下来:**`recv(n)` 返回的是「内核现在手头有的字节」,可能少于 n,"
            "也可能是好几条消息粘在一起。** 正确的 TCP 读法永远是「按协议边界循环读」,"
            "绝不能写 `data = recv(1024)` 然后假设 `data` 正好是一条完整消息。",
            "TCP 读取的黄金法则"
        ),

        biz(
            "你用的所有框架都在背后做这件事:\n"
            "- **Redis 客户端** 按 RESP 协议(`$6\\r\\nfoobar\\r\\n` —— 长度前缀 + 分隔符混用)分帧。\n"
            "- **HTTP 库** 先按 `\\r\\n\\r\\n` 找到头结束,再按 `Content-Length` 或 `chunked` 读体。\n"
            "- **gRPC** 用 5 字节前缀(1 字节压缩标志 + 4 字节长度)分帧。\n\n"
            "所以当你看到一个新协议,第一个该问的问题就是:**「它怎么划消息边界?」** "
            "长度前缀还是分隔符 —— 答案就那两个。",
            "你天天用的框架都在分帧"
        ),
    ]

    boss = [
        q("`recv(4096)` 返回了 1000 字节。关于这 1000 字节,正确的理解是?",
          ["一定是一条完整的消息",
           "是对端某一次 send 的完整内容",
           "只是内核当前缓冲区里有的字节,可能是半条消息,也可能是几条消息粘在一起",
           "说明对端只发了 1000 字节"],
          2,
          "TCP 是字节流,recv 返回的字节数与对端 send 的次数、每次的大小都没有对应关系。"
          "它只是「内核现在能给你的」。所以必须靠应用层协议来划边界。"),

        q("用 4 字节长度前缀分帧,发送时长度字段为什么要 `htonl` / 大端编码?",
          ["为了加密",
           "长度字段本身是多字节整数,收发两端字节序可能不同,必须统一成网络序",
           "为了压缩",
           "不需要,长度字段可以随便发"],
          1,
          "长度头是个 4 字节整数,同样面临字节序问题。若发送方小端直接发、接收方大端解析,"
          "长度会被读成一个巨大的错误值,直接读崩。统一用网络序(大端)是铁律。"),

        q("下面哪种读法是**正确**的长度前缀协议读取?",
          ["data = recv(4096);  取前4字节当长度,剩下当内容",
           "先循环 recv 直到读满 4 字节得到长度 N,再循环 recv 直到读满 N 字节",
           "recv 一次,如果不够就报错让对方重发",
           "用固定大小的包,不需要长度前缀"],
          1,
          "必须分两步、每步都循环读够:先读满 4 字节头,解析出长度 N,再读满 N 字节体。"
          "任何「假设一次 recv 就够」的写法在大消息或高负载下都会出错。Go 的 io.ReadFull "
          "就是这个循环的封装。"),
    ]

    write("L03", "消息的边界要你自己划",
          "TCP 是字节流不是消息流。发明一个划边界的协议,你就理解了 HTTP/Redis/gRPC 的第一层。",
          blocks, boss,
          boss_title="通关测试:分帧",
          unlocks="- 你现在具备了读懂**任何**应用层协议的基础。下一幕的 HTTP 就是一个具体的分帧协议。")


# ================================================================ L04
def L04():
    blocks = [
        hook(
            "你的 `tcp_server` 是单线程串行的:必须等当前客户端断开,才能服务下一个。"
            "实验三里你已经看到,第 4 个客户端就开始在 backlog 队列里排队了。\n\n"
            "真实服务器要同时伺候成千上万人。「一个进程怎么同时处理一万个连接」—— "
            "这就是著名的 **C10K 问题**。它的解法演化史,就是半部服务器架构史。"
        ),

        h("四代解法,一张图看懂差别"),
        fig("concurrency", "同样 6 个请求(每个含 IO 等待),四种模型的耗时对比"),

        p("看图:串行是一条长龙,总耗时是所有请求之和。而 epoll 单线程却能让请求几乎同时推进 —— "
          "**因为大部分时间连接是在等 IO(等网络、等数据库),这段等待时间被复用了。**"
          "这是理解现代服务器的钥匙。"),

        table(
            ["模型", "怎么做", "问题", "谁用"],
            [
                ["串行", "accept 一个,处理完,再 accept 下一个", "一次只能一个人,其余排队", "你的教学版 / 极简场景"],
                ["**每连接一进程** fork", "来一个连接 fork 一个子进程", "进程开销大,几百个就吃不消", "早期 Apache (prefork)"],
                ["**每连接一线程**", "来一个连接开一个线程", "线程比进程轻,但上万个线程调度就崩了", "传统 Java(Tomcat BIO)"],
                ["**epoll 事件循环**", "单线程用 epoll 同时盯着所有 fd,谁就绪处理谁", "编程模型复杂,一个阻塞调用毁所有", "nginx、Redis、Node.js"],
            ],
        ),

        key(
            "**C10K 的难点从来不是「开一万个连接」,而是「一个线程怎么知道这一万个里哪个有数据了」。**\n\n"
            "- `select`/`poll` 的做法:每次都把一万个 fd 全问一遍「你好了吗」—— O(n),连接越多越慢。\n"
            "- `epoll` 的做法:内核维护一个就绪列表,**只把好了的 fd 告诉你** —— O(就绪数),和总连接数无关。\n\n"
            "这就是 nginx 能用几个进程扛几十万连接,而传统一连接一线程的服务器几千就崩的根本区别。",
            "epoll 到底赢在哪"
        ),

        lab(
            "backlog", "重现连接在队列里排队",
            "跑一个 backlog=2、而且**永不 accept** 的服务器,然后连 5 个客户端进去。"
            "你会看到前几个 connect 成功、后面的卡住 —— 亲眼看到那个「握完手却没人服务」的队列。",
            script="l04_backlog",
            ask="服务器一次 accept 都不调,客户端的 connect 会成功吗?",
            expect=(
                "listen(backlog=2) 完成,故意不 accept,睡 30 秒\n"
                "第 1 个客户端: connect成功=1\n"
                "第 2 个客户端: connect成功=1\n"
                "第 3 个客户端: connect成功=1\n"
                "第 4 个客户端: connect成功=0     <- 卡住\n"
                "第 5 个客户端: connect成功=0     <- 卡住\n\n"
                "State   Recv-Q  Send-Q  Local Address:Port\n"
                "LISTEN  3       2       0.0.0.0:*\n"
            ),
            explain=(
                "服务器**一次 accept 都没调**,前 3 个客户端的 `connect()` 却成功了 —— "
                "**这证明三次握手是内核自动完成的,和你的 accept 无关。** "
                "accept 只是「从内核已握完手的队列里取一个出来」。\n\n"
                "`ss` 里那两个数字很重要:\n"
                "- `Recv-Q = 3`:全连接队列里当前**堆积**了 3 个等着被 accept 的连接。\n"
                "- `Send-Q = 2`:这个队列的**上限**(就是 backlog)。\n\n"
                "**线上告警:`Recv-Q` 持续接近 `Send-Q`,就是「accept 不过来 / 服务过载」的信号。**"
                "(backlog=2 却进了 3,是 Linux `backlog+1` 的实现细节,别依赖精确值。)"
            ),
        ),

        h("为什么「一个阻塞调用毁所有」"),
        p("事件循环的致命弱点:因为**一个线程管所有连接**,只要你在处理某个连接时调用了一个"
          "**阻塞**的操作(比如同步读文件、同步查数据库、`time.sleep`),整个循环就卡住了,"
          "其余上万个连接**全部**跟着饿死。"),

        poly(
            title="同一个陷阱,在事件循环模型里",
            py=(
                "import asyncio\n\n"
                "async def handle(reader, writer):\n"
                "    data = await reader.read(1024)\n"
                "    # ✗ 致命:同步阻塞调用,卡住整个事件循环\n"
                "    # result = requests.get('http://slow-api')   # 别这样!\n"
                "    # ✓ 正确:用异步版本,让出控制权\n"
                "    result = await async_http_get('http://slow-api')\n"
                "    writer.write(result)\n"
                "    await writer.drain()",
                "asyncio_trap.py",
                "Python asyncio 里混入一个同步阻塞调用,是最常见的性能事故。"
                "`async`/`await` 的本质就是「在等 IO 时主动让出线程」。"
            ),
            go=(
                "// Go 的杀手锏:你根本不用手写事件循环\n"
                "for {\n"
                "    conn, _ := ln.Accept()\n"
                "    go handle(conn)      // 每连接一个 goroutine,就这一行\n"
                "}\n\n"
                "func handle(conn net.Conn) {\n"
                "    // 这里可以放心写「阻塞」代码:\n"
                "    result := http.Get(\"http://slow-api\")  // 看起来阻塞\n"
                "    // 但 Go runtime 会在它等 IO 时把这个 goroutine 挪开,\n"
                "    // 底层用 epoll 复用线程 —— 你写同步代码,拿到异步性能\n"
                "}",
                "goroutine.go",
                "**Go 最大的价值在这里**:你用最直觉的「一连接一 goroutine + 同步代码」写法,"
                "Go runtime 底层却用 epoll 事件循环 + 少量 OS 线程来跑。"
                "既没有回调地狱,又没有一线程一连接的开销。"
            ),
            cpp=(
                "// C/C++ 手写 epoll 事件循环的骨架\n"
                "int ep = epoll_create1(0);\n"
                "epoll_ctl(ep, EPOLL_CTL_ADD, lfd, &ev);   // 把监听 fd 加进去\n"
                "for (;;) {\n"
                "    int n = epoll_wait(ep, events, MAX, -1);  // 只返回就绪的\n"
                "    for (int i = 0; i < n; i++) {\n"
                "        if (events[i].data.fd == lfd) accept_new(ep);\n"
                "        else handle_ready(events[i].data.fd);  // 绝不能阻塞!\n"
                "    }\n"
                "}",
                "epoll_loop.c",
                "这就是 nginx / Redis 主循环的骨架。`epoll_wait` 只返回就绪的 fd,"
                "是整个高性能的核心。代价:handle 里任何阻塞调用都会卡死全场。"
            ),
        ),

        biz(
            "选型不是「哪个最先进用哪个」,而是看**你的活儿是 CPU 密集还是 IO 密集**:\n\n"
            "- **IO 密集**(绝大多数 Web 后端:大部分时间在等数据库、等下游接口)→ "
            "事件循环 / 协程最划算,少量线程扛海量连接。nginx、Node、Go、Redis 都是这条路。\n"
            "- **CPU 密集**(视频编码、大量计算)→ 事件循环没优势,反而要**多进程 / 多线程**"
            "把多核吃满。\n\n"
            "**实际生产常常是组合**:nginx(epoll)在前面扛连接,后面接一堆多进程 / 多线程的"
            "worker 干活。你后面几关搭的就是这个结构。",
            "怎么选:先问 CPU 密集还是 IO 密集"
        ),
    ]

    boss = [
        q("服务器一次 `accept` 都不调用,客户端 `connect` 却成功了。这说明?",
          ["客户端在骗人,其实没连上",
           "三次握手由内核自动完成,accept 只是从已完成队列里取连接,不参与握手",
           "这是操作系统的 bug",
           "connect 有重试机制"],
          1,
          "listen() 之后内核就自动替你握手,握完的连接放进全连接队列。accept 只是"
          "「取货」。所以不 accept,connect 照样成功,连接堆在队列里没人服务 —— "
          "这正是 backlog 存在的意义。"),

        q("`epoll` 相比 `select` 在处理海量连接时的核心优势是?",
          ["epoll 用了更多线程",
           "select 有 bug",
           "select 每次要遍历全部 fd(O(n)),epoll 只返回就绪的 fd(与总连接数无关)",
           "epoll 能突破文件描述符上限"],
          2,
          "两者都是单线程管多 fd,区别在效率:select/poll 每次调用都要把所有 fd 检查一遍,"
          "连接越多越慢;epoll 靠内核就绪列表,只把有事件的 fd 交给你。这是 C10K 的关键。"),

        q("一个用 asyncio / 事件循环写的服务,你在某个请求处理里加了一句同步的 `time.sleep(2)`。后果?",
          ["只有这个请求慢 2 秒,其他不受影响",
           "整个事件循环被卡住 2 秒,期间所有连接都得不到处理",
           "asyncio 会自动把它转成异步",
           "会开一个新线程去 sleep"],
          1,
          "事件循环是单线程的。一个同步阻塞调用会霸占这个唯一的线程,导致期间所有连接饿死。"
          "这是事件循环模型最致命也最常犯的错。必须用 await asyncio.sleep 之类的异步版本。"),

        q("一个典型的 IO 密集型 Web 后端(大量时间在等数据库),最合适的并发模型是?",
          ["每连接一进程 fork",
           "串行处理保证顺序",
           "事件循环 / 协程(如 Go goroutine、Node、asyncio)",
           "每个请求开 100 个线程"],
          2,
          "IO 密集意味着线程大部分时间在等,这段等待可以被复用去处理别的连接。"
          "事件循环/协程正是干这个的,能用极少线程扛海量并发连接。CPU 密集才需要靠多进程吃满多核。"),
    ]

    write("L04", "一个进程,一万个连接",
          "C10K 的解法演化史就是半部服务器架构史。理解 epoll,就理解了 nginx/Redis/Node 的心脏。",
          blocks, boss,
          boss_title="通关测试:并发模型",
          unlocks="- **应用 #1** 节点点亮 —— 你的服务器现在能同时服务很多连接了。\n"
                  "- 第一幕完成:你已经掌握了字节如何可靠地在两台机器间流动。")


if __name__ == "__main__":
    print("第一幕:")
    L01(); L02(); L03(); L04()
