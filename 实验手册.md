# 动手实验手册

7 个实验,每个都是"改一行代码 / 换一个参数,看现象变化"。
下面所有「实测输出」都是在本机真跑出来的,不是编的 —— 你应该看到同样的结果。

准备:

```bash
make            # 编译
```

约定:**终端1** 跑服务器,**终端2** 跑客户端。

---

## 实验一:服务器不 bind 会怎样?

**要验证的认知:** `bind` 不是"必须调用的仪式",而是"我要固定在哪个端口"。
不 bind 不会报错,内核会给你随机分一个 —— 但客户端就找不到你了。

把 `c/tcp_server.c` 里的 `bind(...)` 那几行注释掉,加上打印:

```c
/* if (bind(lfd, (struct sockaddr *)&srv, sizeof(srv)) < 0) { ... } */

listen(lfd, BACKLOG);
struct sockaddr_in me; socklen_t ml = sizeof(me);
getsockname(lfd, (struct sockaddr *)&me, &ml);
printf("我实际在监听端口: %d\n", ntohs(me.sin_port));
```

**实测:**

```
不bind直接listen  -> ret=0, 内核分配的端口=52299
```

`listen()` **成功了**,端口是内核随机给的 52299(每次运行都不同)。
服务器在正常工作,只是客户端连 8888 会被拒绝 —— 因为服务器压根不在 8888。

**结论:** `bind` 回答的是"我在哪"。服务器必须 bind 是因为**客户端得能预先
知道往哪连**,不是因为技术上必须。客户端不 bind,正是因为没人需要主动找它。

---

## 实验二:不 listen 直接 accept?对 UDP 调 listen?

**要验证的认知:** `listen` 把套接字从"主动"改成"被动"。没这一步,
内核不认为你是服务器。UDP 则根本没有"被动"这个状态。

```bash
gcc -x c -o /tmp/e2 - <<'EOF' && /tmp/e2
#include <stdio.h>
#include <string.h>
#include <errno.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
int main(void){
    int r;
    int u = socket(AF_INET, SOCK_DGRAM, 0);
    errno=0; r=listen(u,5);
    printf("UDP  listen()               -> ret=%d errno=%d %s\n", r, errno, strerror(errno));
    errno=0; r=accept(u,NULL,NULL);
    printf("UDP  accept()               -> ret=%d errno=%d %s\n", r, errno, strerror(errno));
    int t = socket(AF_INET, SOCK_STREAM, 0);
    struct sockaddr_in a; memset(&a,0,sizeof a);
    a.sin_family=AF_INET; a.sin_port=htons(17779); a.sin_addr.s_addr=htonl(INADDR_ANY);
    bind(t,(struct sockaddr*)&a,sizeof a);
    errno=0; r=accept(t,NULL,NULL);
    printf("TCP  bind了但不listen就accept -> ret=%d errno=%d %s\n", r, errno, strerror(errno));
    return 0;
}
EOF
```

**实测输出:**

```
UDP  listen()               -> ret=-1 errno=95 Operation not supported
UDP  accept()               -> ret=-1 errno=95 Operation not supported
TCP  bind了但不listen就accept -> ret=-1 errno=22 Invalid argument
```

**结论:**

- UDP 的 `listen`/`accept` 返回 **EOPNOTSUPP(95)** —— 内核明确说"数据报套接字
  不支持这操作"。不是你写错了,是**概念上就不存在**。
- TCP 不 listen 就 accept 返回 **EINVAL(22)** —— 因为此时 fd 还是"主动"套接字,
  它身上根本没有连接队列可供 accept 去取。

顺便记两个 bind 的常见错误(同一段代码可验证):

```
同一端口 bind 两次   -> errno=98  Address already in use
非root bind 80 端口  -> errno=13  Permission denied     (端口 <1024 需要特权)
```

---

## 实验三:accept 不调用,客户端还能连上吗?★ 最反直觉

**要验证的认知:** **三次握手是内核自动完成的,和你调不调 `accept` 无关。**
`accept` 只是"从已完成握手的队列里取一个出来"。

写一个 backlog 只有 2、并且**永远不 accept** 的服务器:

```bash
gcc -x c -o /tmp/e3 - <<'EOF'
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
int main(void){
    int l=socket(AF_INET,SOCK_STREAM,0); int on=1;
    setsockopt(l,SOL_SOCKET,SO_REUSEADDR,&on,sizeof on);
    struct sockaddr_in a; memset(&a,0,sizeof a);
    a.sin_family=AF_INET; a.sin_port=htons(17778); a.sin_addr.s_addr=htonl(INADDR_ANY);
    bind(l,(struct sockaddr*)&a,sizeof a);
    listen(l,2);                       /* backlog=2,而且下面【永不 accept】 */
    printf("listen(backlog=2) 完成,故意不 accept,睡 30 秒\n");
    sleep(30);
    return 0;
}
EOF
/tmp/e3 &
sleep 0.5
for i in 1 2 3 4 5; do
  timeout 2 ./bin/tcp_client 127.0.0.1 17778 --noread > /tmp/cl$i.log 2>&1 &
  sleep 0.3
  echo "第 $i 个客户端: connect成功=$(grep -c 'connect() 成功' /tmp/cl$i.log)"
done
ss -tanl | grep 17778
```

**实测输出:**

```
第 1 个客户端: connect成功=1
第 2 个客户端: connect成功=1
第 3 个客户端: connect成功=1
第 4 个客户端: connect成功=0     <- 卡住了,连不上
第 5 个客户端: connect成功=0     <- 卡住了

State   Recv-Q  Send-Q  Local Address:Port
LISTEN  3       2       0.0.0.0:17778
```

**这里有三个关键点:**

1. **服务器一次 `accept` 都没调用,前 3 个客户端的 `connect()` 却成功返回了。**
   这就证明了握手是内核干的。客户端以为"连上了",实际上服务器**根本不知道
   它们的存在** —— 它们只是躺在内核的全连接队列里。

2. **`backlog=2` 却进了 3 个。** Linux 上实际容量是 `backlog+1`。
   这个 off-by-one 是历史实现细节,不同系统不一样,所以**永远不要依赖
   backlog 的精确数值**。

3. **`ss -tanl` 里 LISTEN 行的两个队列列:**
   - `Recv-Q` = 3 → 全连接队列里**当前堆积**了 3 个等着被 accept 的连接
   - `Send-Q` = 2 → 这个队列的**上限**(就是你传给 listen 的 backlog)

   `Recv-Q` 持续接近 `Send-Q`,就是"服务器 accept 不过来了"的经典告警信号。

**结论:** `connect()` 成功 ≠ 服务器在服务你。这是理解 backlog 和
"惊群"、"服务器过载"类问题的基础。

---

## 实验四:TIME_WAIT 和 SO_REUSEADDR

**要验证的认知:** 主动关闭连接的一方会进入 TIME_WAIT ~60 秒,
期间它占用的那个端口不能被再次 bind。

```bash
./bin/tcp_server 17781 &
sleep 0.4
./bin/tcp_client 127.0.0.1 17781 "bye"     # 客户端先 close,所以客户端是主动关闭方
sleep 0.3
ss -tan | grep 17781
```

**实测输出:**

```
LISTEN     0  128     0.0.0.0:17781    0.0.0.0:*
TIME-WAIT  0  0     127.0.0.1:48116  127.0.0.1:17781
```

TIME-WAIT 那行的 **Local Address 是客户端的临时端口 48116**,不是服务器的
17781。这证明了:**谁先 `close()`,谁进 TIME_WAIT**。

**再验证 SO_REUSEADDR 的作用:**

把 `c/tcp_server.c` 里这三行注释掉:

```c
/* int on = 1;
   if (setsockopt(lfd, SOL_SOCKET, SO_REUSEADDR, &on, sizeof(on)) < 0)
       perror("setsockopt"); */
```

重新编译,然后:让**服务器**先关闭(让服务器成为主动关闭方),
再立刻重启服务器:

```bash
make
./bin/tcp_server 17782 &
sleep 0.3; ./bin/tcp_client 127.0.0.1 17782 hi; sleep 0.3
kill %1                      # 杀掉服务器
./bin/tcp_server 17782       # 立刻重启
```

不带 SO_REUSEADDR 时会看到:

```
bind: Address already in use
```

**为什么 TCP 要有 TIME_WAIT?** 两个原因:
1. 最后那个 ACK 可能丢了,对方会重发 FIN,你得还在那儿才能再回一个 ACK。
2. 防止上一个连接的"迷路"数据包,在同样的四元组被复用后串到新连接里。

`SO_REUSEADDR` 的作用是:允许 bind 一个处于 TIME_WAIT 的本地地址。
**所有服务器程序都应该设这个选项** —— 否则你的服务重启一次就要等一分钟。

---

## 实验五:TCP 粘包 ★ 必做

**要验证的认知:** TCP 是**字节流**,不保证消息边界。

```bash
./bin/tcp_server 18890 &
sleep 0.4
./bin/tcp_client 127.0.0.1 18890 --burst 5
```

**实测 —— 客户端侧:**

```
    send() "PKT-00" (6 字节)
    send() "PKT-01" (6 字节)
    send() "PKT-02" (6 字节)
    send() "PKT-03" (6 字节)
    send() "PKT-04" (6 字节)

[5] 只 recv() 了【一次】,收到 30 字节: "PKT-00PKT-01PKT-02PKT-03PKT-04"
```

**服务器侧只 recv 了 1 次:**

```
    recv() = 30 字节: "PKT-00PKT-01PKT-02PKT-03PKT-04"
```

**5 次 `send` → 1 次 `recv`。** 消息边界彻底消失了。

Python 版本 (`python3 py/tcp_client.py 127.0.0.1 18890 --burst 5`) 结果完全一样 ——
这不是语言问题,是 TCP 协议本身的性质。

**现在换成 UDP 做同样的事,对比一下:** UDP 永远不会粘包,
`sendto` 5 次,`recvfrom` 就是 5 次,一次一个包,一字节不多不少。

**结论与实践:** TCP 应用层**必须自己定义消息边界**,两种主流做法:
1. **长度前缀**:先发 4 字节表示长度,再发内容。二进制协议标配。
2. **分隔符**:用 `\n` 之类分隔。HTTP 头、Redis 协议就是这么干的。

顺便:反过来的"拆包"也会发生 —— 发 10KB,接收方可能要 `recv` 好几次才收全。
所以正确的 TCP 读法永远是"循环读到够为止",不能假设一次 `recv` 拿到完整消息。

---

## 实验六:UDP 数据报截断

**要验证的认知:** UDP 缓冲区不够时,**超出部分被静默丢弃**,不报错、不保留。

```bash
./bin/udp_server 19997 &            # 服务器 buf 是 1024 字节
sleep 0.4
./bin/udp_client 127.0.0.1 19997 --big 2000
```

**实测 —— 客户端发了 2000 字节:**

```
[2] sendto() 一个 2000 字节的数据报
    sendto 返回 2000
```

**服务器只收到 1023 字节:**

```
[4] recvfrom() 收到 1023 字节,来自 127.0.0.1:39874: "AAAA...(1023个A)"
```

(1023 而不是 1024,是因为代码里写的是 `sizeof(buf) - 1`,留了一个字节放 `\0`)

**剩下的 977 字节永远消失了**,而且 `recvfrom` **没有任何报错**,
下次 `recvfrom` 拿到的是**下一个数据报**,不是这个包的后半截。

对比 TCP:TCP 缓冲区不够时,剩下的字节还在内核里,下次 `recv` 接着拿。

**怎么发现截断?** 加 `MSG_TRUNC` 标志:

```c
ssize_t n = recvfrom(fd, buf, sizeof(buf), MSG_TRUNC, ...);
/* 加了 MSG_TRUNC 后,返回值是【数据报的真实长度】(2000),
   而不是实际拷贝进 buf 的长度(1024)。
   于是 n > sizeof(buf) 就说明发生了截断。 */
```

**结论:** UDP 收缓冲区要按**协议允许的最大数据报**来开。
保守做法是开 65536(UDP 数据报理论最大值)。

---

## 实验七:UDP 的 connect —— 它到底做了什么?

**要验证的认知:** UDP 也能 `connect`,但它**不发任何网络包**,
只是在内核里记一个默认对端。

**先看不 connect 时的问题**(服务器故意不启动):

```bash
./bin/udp_client 127.0.0.1 19123 "hello"     # 19123 上没有任何服务
```

**实测:**

```
[2] sendto() "hello" -> 127.0.0.1:19123
    发出 5 字节 (注意:返回成功≠对方收到)
[3] 2 秒超时,没收到回复。
    可能:服务器没起 / 包丢了 / 回包丢了。
    UDP 不会告诉你是哪一种 —— 这就是「不可靠」。
```

`sendto` **返回成功**,尽管对面根本没人。只能靠超时猜发生了什么。

**再看 connect 之后:**

```bash
./bin/udp_client 127.0.0.1 19123 --connect
```

**实测:**

```
[3] send() 24 字节 (不用 sendto 了,对端已记住)
[4] ECONNREFUSED —— 收到了 ICMP 端口不可达。
    这个错误【只有 connect 过的 UDP 才拿得到】
```

**同样是对面没人,现在拿到了明确的 ECONNREFUSED。**

原因:对方主机的内核收到发往无人监听端口的 UDP 包,会回一个
**ICMP Port Unreachable**。但没 connect 的 socket 收到这个 ICMP 时,
内核不知道该把错误报告给哪次 `sendto`(你可能同时在给 10 个不同地址发包),
所以只能丢弃。connect 过之后有了唯一对端,内核就能把错误关联上来了。

**UDP connect 做的三件事:**

1. 记下默认对端 → 之后可以用 `send`/`recv` 代替 `sendto`/`recvfrom`
2. **过滤**:只接收这个对端发来的包,其他来源的直接丢弃
3. 能收到 ICMP 错误(如上)

**不做的事:** 不发包、不握手、不建立任何"连接"。它纯粹是本地内核状态。
你可以对同一个 UDP socket 反复 connect 到不同地址,随时改主意。

---

## 做完之后

回头再看 [README.md](README.md) 里的那张对照表,
`bind` / `listen` / `accept` / `connect` / `send` / `recv` 这六个词
应该已经不是"要背的 API 顺序",而是六个你亲眼见过其后果的具体动作了。

如果还想继续深入,下一站是**并发**:
把 `tcp_server.c` 的主循环改成 `fork()` 每连接一进程,
再改成 `epoll` 单线程管所有连接。实验三里那个堆积的 backlog 队列,
就是理解这一步的最好起点。
